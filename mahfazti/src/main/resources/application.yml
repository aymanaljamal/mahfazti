
spring:
  application:
    name: mahfazti

  datasource:
    url: jdbc:mysql://localhost:3306/mahfazti_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ayman@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  jackson:
    time-zone: Asia/Jerusalem


server:
  port: 8080


jwt:
  secret: YOUR_SUPER_SECRET_JWT_KEY_CHANGE_THIS_IN_PRODUCTION
  expiration: 86400000
